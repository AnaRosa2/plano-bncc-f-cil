# .env.example for backend
# Copy this file to `.env` and fill in your secrets. NEVER commit `.env` with real credentials.

# Gemini API key used by Google Generative AI client
GEMINI_API_KEY=

# Frontend allowed origin (used for CORS in development)
FRONTEND_URL=http://localhost:5173

# App settings
NODE_ENV=development
PORT=3333
